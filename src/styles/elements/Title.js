import styled from 'styled-components';

const Title = styled.h1`
  font-size: 2rem;
  color: var(--green-primary);
  text-transform: uppercase;
  text-shadow: -1px 2px 0 var(--green-second);
`;

export default Title;
